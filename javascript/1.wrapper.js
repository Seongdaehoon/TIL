// Wrapper Object (built-in)
// 원시값을 관련된 빌트인 "객체"로 변환한다.
const number = 123;

console.log(number.toString());
// number 원시타입을 감싸고 있는 Number 객체로 바뀜

const text = "text";
console.log(text.length);
text.trim();
